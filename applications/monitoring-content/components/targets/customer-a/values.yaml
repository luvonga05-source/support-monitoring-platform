config:
  datasource:
    host: "164.92.179.57"
    port: "5432"
    database: "postgres"
    sslmode: "disable"   # or verify-full / disable, etc.
    userSecret:
      name: "pgx-conn"
      key: "username"
    passwordSecret:
      name: "pgx-conn"
      key: "password"

serviceMonitor:
  enabled: true
  labels:
    monitoring.cybertec.at/scope: customers
  additionalLabels:
    monitoring.cybertec.at/scope: customers
